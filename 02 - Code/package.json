{
  "name": "w2group4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker": "docker build -t example2 . && docker tag example2 localhost:5000/example2 && docker push localhost:5000/example2",
    "k8s-deploy": "kubectl apply -f kubernetes/namespace.yaml && kubectl apply -f kubernetes/deployment.yaml && kubectl apply -f kubernetes/service.yaml && kubectl rollout restart -n jkpgcity deployment example2-deployment",
    "k8s-run": "kubectl port-forward svc/example2-service 4000:3000 -n jkpgcity"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
